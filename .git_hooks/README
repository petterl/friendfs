These are various git hooks which are recommended by us. 
To install them in a your repository execute the following 
commands:

cd .git/
rm -rf hooks
ln -s ../.git_hooks hooks
cd ..

Stolen from the Arora project: 
http://github.com/Arora/arora/tree/master/git_hooks/
